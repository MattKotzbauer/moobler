Create a brief practice challenge for tmux keybind "{{keybind}}" which does "{{command}}".

The challenge should:
- Give a clear, specific objective the user can accomplish
- Be completable in under a minute
- Help build muscle memory for the keybind

Return JSON: {"objective": "...", "hint": "..."}
